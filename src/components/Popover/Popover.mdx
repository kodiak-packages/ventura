---
name: Dropdown menu
menu: Components
route: /dropdown-menu
---

import { Playground, Props } from 'docz';
import { Popover, Menu, Button } from '../../index.ts';

# Popover

A Popover is used to display extra information

## Best practices

- Check

## Examples

### Basic usage

<Playground>
  {() => {
    const [isVisible, setIsVisible] = React.useState(false);
    const popoverContent = (<p>Hello</p>);
    return (
      <>
        <Popover isVisible={isVisible} setIsVisible={setIsVisible} content={popoverContent}>
          <Button onClick={() => setIsVisible(!isVisible)}>Open basic modal</Button>
        </ Popover>
        <br />
        <br />
        <br />
      </>
    );
  }}

</Playground>

## API

<Props of={Popover} />
